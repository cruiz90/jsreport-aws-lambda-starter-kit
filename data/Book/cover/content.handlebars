<html>

<head>
    <meta charset="UTF-8">
    <style>
        * {
            box-sizing: border-box;
        }
        html,
        body {
            margin: 0px;
            padding: 0px;
            font-family: Arial, serif;
            font-size: 12px;
        }
        .cover {
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            width: 100%;
            height: 100%;
        }
        .cover-content {
            display: flex;
            flex-direction: column;
            flex-grow: 1;
            align-items: center;
            justify-content: center;
        }
    </style>
</head>

<body>
    <main class="cover">
        {{{pdfAddPageItem}}}
        <div class="cover-header">
            <img src="{{asset "nvidia-logo-vert.png" "dataURI" }}" height="100" alt="logo" />
        </div>
        <div class="cover-content">
            <h1 class="cover-title">{{result.title}}</h1>
            <br />
            <h2 class="cover-section">{{result.section}}</h2>
            <br />
            <h3 class="cover-version">{{result.version}}</h3>
        </div>
        </div>
    </main>
</body>

</html>